{
  "name": "omfk-social",
  "version": "1.0.0",
  "description": "OMFK Social monorepo",
  "private": true,
  "scripts": {
    "install:all": "npm install && npm install --prefix backend && npm install --prefix frontend",
    "dev": "node scripts/dev-banner.mjs && concurrently -n backend,frontend -c blue,green \"npm run dev:backend\" \"npm run dev:frontend\"",
    "start": "npm run dev",
    "dev:backend": "npm run start:dev --prefix backend",
    "dev:frontend": "npm run dev --prefix frontend",
    "build": "npm run build --prefix backend && npm run build --prefix frontend",
    "test": "npm run test --prefix backend"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/AbetikDev/OMFK-Social.git"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/AbetikDev/OMFK-Social/issues"
  },
  "homepage": "https://github.com/AbetikDev/OMFK-Social#readme",
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}